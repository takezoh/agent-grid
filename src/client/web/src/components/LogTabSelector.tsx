// DEPRECATED: replaced by LogTabs in A1-δ; this shim exists so App.tsx still
// compiles until ts-app-wiring removes the import. Remove the file entirely
// after that.
export { LogTabs as LogTabSelector } from "./LogTabs";
